group = "org.j2c"
version = "0.1.0-alpha"

tasks.register("prepareCMake", Exec) {
    workingDir "$projectDir/build"
    commandLine "cmake", ".."
}
tasks.register("buildNative", Exec) {
    workingDir "$projectDir/build"
    commandLine "make"
}
buildNative.dependsOn(prepareCMake)